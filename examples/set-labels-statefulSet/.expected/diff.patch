diff --git a/resources.yaml b/resources.yaml
index 91ddb27..aa5cd5e 100644
--- a/resources.yaml
+++ b/resources.yaml
@@ -2,10 +2,15 @@ apiVersion: apps/
 kind: StatefulSet
 metadata:
   name: web
+  labels:
+    color: orange
+    fruit: apple
 spec:
   selector:
     matchLabels:
       app: nginx # has to match .spec.template.metadata.labels
+      color: orange
+      fruit: apple
   serviceName: "nginx"
   replicas: 3 # by default is 1
   minReadySeconds: 10 # by default is 0
@@ -14,6 +19,8 @@ spec:
         name: www
         labels:
           key: www
+          color: orange
+          fruit: apple
       spec:
         accessModes: ["ReadWriteOnce"]
         storageClassName: "my-storage-class"
@@ -24,9 +31,16 @@ spec:
         name: xxx
         labels:
           key: xxx
+          color: orange
+          fruit: apple
         spec:
           accessModes: ["ReadWrite"]
           storageClassName: "my-storage-class"
           resources:
             requests:
-              storage: 1Gi
\ No newline at end of file
+              storage: 1Gi
+  template:
+    metadata:
+      labels:
+        color: orange
+        fruit: apple