apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
  annotations:
    config.k8s.io/function: |
      container:
        image: gcr.io/kpt-fn/set-label:v0.1
    config.kubernetes.io/local-config: "true"
data:
  color: orange
  fruit: apple
