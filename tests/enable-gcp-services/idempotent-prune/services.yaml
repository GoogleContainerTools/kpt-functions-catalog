apiVersion: blueprints.cloud.google.com/v1alpha1
kind: ProjectServiceSet
metadata:
  name: proj1-service
  annotations:
    config.kubernetes.io/local-config: "true"
spec:
  services: # We expect only generated service resources by proj1-service ProjectServiceSet to be pruned and regenarated. All other service resources remain unmodified.
    - compute.googleapis.com
    - redis.googleapis.com
  projectID: proj1
