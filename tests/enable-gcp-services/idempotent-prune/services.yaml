apiVersion: blueprints.cloud.google.com/v1alpha1
kind: ProjectServiceList
metadata:
  name: proj1-service
spec:
  services: # We expect only generated service resources by proj1-service ProjectServiceList to be pruned and regenarated. All other service resources remain unmodified.
    - compute.googleapis.com
    - redis.googleapis.com
  projectID: proj1
