apiVersion: apps/v1
kind: Deployment
metadata:
  name: base-app
  namespace: example
  labels:
    test: abstract-base-app
    app.kubernetes.io/name: frontend
spec:
  selector:
    matchLabels:
      test: abstract-base-app
      app.kubernetes.io/name: frontend
  template:
    metadata:
      labels:
        test: abstract-base-app
        app.kubernetes.io/name: frontend